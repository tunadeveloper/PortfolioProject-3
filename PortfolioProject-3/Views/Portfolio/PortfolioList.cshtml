@using PortfolioProject_3.DataAccess.Entities
@model List<Portfolio>
@{
    ViewData["Title"] = "PortfolioList";
    Layout = "~/Views/Layout/Index.cshtml";
    int count = 0;
}

<div class="main-panel">
    <div class="content">
        <div class="container-fluid">
            <h4 class="page-title">Portfolyolar</h4>
            <div class="row">
                <div class="col-md-12">
                    <div class="card">
                        <div class="card-body">
                            <table class="table mt-3">
                                <thead>
                                    <tr style="background-color: #E0BBE4;">
                                        <th scope="col">#</th>
                                        <th scope="col">Başlık</th>
                                        <th scope="col">Alt Başlık</th>
                                        <th scope="col">Görsel</th>
                                        <th scope="col">Link</th>
                                        <th scope="col">Açıklama</th>
                                        <th scope="col">Sil</th>
                                        <th scope="col">Güncelle</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @foreach (var item in Model)
                                    {
                                        count++;
                                        <tr>
                                            <td>@count</td>
                                            <td>@item.Title</td>
                                            <td>@item.Subtitle</td>
                                            <td>
                                                <img src="@item.ImageUrl" alt="@item.Title" style="width:60px;height:60px;object-fit:cover;" />
                                            </td>
                                            <td><a href="@item.Url" target="_blank">@item.Url</a></td>
                                            <td>@item.Description</td>
                                            <td><a href="/Portfolio/Delete/@item.Id" class="btn btn-danger">Sil</a></td>
                                            <td><a href="/Portfolio/Update/@item.Id" class="btn btn-success">Güncelle</a></td>
                                        </tr>
                                    }
                                </tbody>
                            </table>
                            <a href="/Portfolio/Create/" class="btn btn-primary">Portfolyo Ekle</a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
