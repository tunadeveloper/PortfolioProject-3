@using PortfolioProject_3.DataAccess.Entities
@model List<Skill>
@{
    ViewData["Title"] = "SkillList";
    Layout = "~/Views/Layout/Index.cshtml";
    int count = 0;
}

<div class="main-panel">
    <div class="content">
        <div class="container-fluid">
            <h4 class="page-title">Yetenekler</h4>
            <div class="row">
                <div class="col-md-12">
                    <div class="card">
                        <div class="card-body">
                            <table class="table mt-3">
                                <thead>
                                    <tr style="background-color: #FFD1DC;">
                                        <th scope="col">#</th>
                                        <th scope="col">Yetenek Adı</th>
                                        <th scope="col">Değer (%)</th>
                                        <th scope="col">Sil</th>
                                        <th scope="col">Güncelle</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @foreach (var item in Model)
                                    {
                                        count++;
                                        <tr>
                                            <td>@count</td>
                                            <td>@item.Title</td>
                                            <td>@item.Value</td>
                                            <td><a href="/Skill/Delete/@item.Id" class="btn btn-danger">Sil</a></td>
                                            <td><a href="/Skill/Update/@item.Id" class="btn btn-success">Güncelle</a></td>
                                        </tr>
                                    }
                                </tbody>
                            </table>
                            <a href="/Skill/Create/" class="btn btn-primary">Yetenek Ekle</a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
